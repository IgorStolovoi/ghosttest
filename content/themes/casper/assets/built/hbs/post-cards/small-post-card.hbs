<a href="{{url}}" class="small-post-card">
	<div class="small-post-card-img">
        {{#if feature_image}}
            <img
                srcset="{{img_url feature_image size="s"}} 300w,
                {{img_url feature_image size="m"}} 600w,
                {{img_url feature_image size="l"}} 1000w,
                {{img_url feature_image size="xl"}} 2000w"
                sizes="(max-width: 1000px) 400px, 800px"
                src="{{img_url feature_image size="m"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                loading="lazy"
            />
        {{/if}}
    </div> 
    <div class="small-post-card-inner">
	    <h4 class="small-post-card-title">{{title words="10"}}</h4>
    	<div class="small-post-card-date"> 
        <time datetime=datetime="{{date format="DD-MM-YYYY"}}">{{date published_at format="DD MMMM, YYYY"}}</time>
        </div>
    </div>
</a>